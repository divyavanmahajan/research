[project]
name = "apartment-finder-backend"
version = "0.1.0"
description = "Stateless Qasa API proxy for the Apartment Finder app"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
