class Skill:
    name = "example"
    description = "Example skill — replace with a real implementation."

    async def run(self, topic: str) -> str:
        return f"Example skill executed for topic: {topic}"
